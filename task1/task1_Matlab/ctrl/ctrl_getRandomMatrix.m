function randMat = ctrl_getRandomMatrix(size)
%In general, you can generate N random numbers in the interval
%[a,b] with the formula r = a + (b-a).*rand(N,1).
rows=size(1);
N=size(2);
random_column 
a=1;
    while a <= rows
    r = a + (b-a).*rand(N,1)
        a=a+1;
    end

end